# Database
POSTGRES_DB=workout
POSTGRES_USER=workout
POSTGRES_PASSWORD=changeme_strong_password
DB_PORT=5435

# Next.js App Port
APP_PORT=3005

# Prisma connection (for local dev - override in docker-compose for container)
DATABASE_URL=postgresql://workout:changeme_strong_password@localhost:5435/workout

# Session secret - must be at least 32 chars, generate with: openssl rand -hex 32
SESSION_SECRET=changeme_random_64_char_hex_string_here_use_openssl_rand_hex_32

# Cloudflare tunnel credentials directory (path to ~/.cloudflared on host)
CLOUDFLARED_CONFIG_DIR=~/.cloudflared

# Public URL (used for CORS and meta tags)
NEXT_PUBLIC_APP_URL=https://gym.yourdomain.com

# Cookie security - set to "true" only when serving over HTTPS (tunnel/production)
COOKIE_SECURE=false

# Web Push (VAPID) — required for iOS background push notifications
# Generate with: npx web-push generate-vapid-keys
# IMPORTANT: NEXT_PUBLIC_VAPID_PUBLIC_KEY must be set before `docker compose build`
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_EMAIL=mailto:admin@yourdomain.com
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
